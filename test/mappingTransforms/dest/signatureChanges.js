function foo(edgeStyle) {
  return edgeStyle.isHit(1, 2)
}

const sns = new yfiles.styles.ShapeNodeStyle(renderer, shape, stroke, fill)

function lessArgs(gmm) {
  return gmm.getCanvasObjectGroup(two);
}

function swap(labelModel) {
  return labelModel.getGeometry(two, one);
}

function swapALot(graphWrapper) {
  graphWrapper.addLabel(one, four, two, three, five, six)
}

function lessArgs2(portCandidateProvider) {
  return portCandidateProvider.getSourcePortCandidates(one, two);
}

function moreArgs(clipBoard) {
  return clipBoard.paste(undefined, one, two, three, undefined);
}

function leaveAddAlone(set) {
  set.add('foo')
}

function dontAdjustPartialRenames(thing) {
  thing.getItem(0)
}
