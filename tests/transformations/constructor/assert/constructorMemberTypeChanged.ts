import { OrganicLayout } from 'yfiles'

const ol = new OrganicLayout({
  /*TODO-Migration Possible breaking change: Type of circleRecognition on OrganicLayout has changed from boolean to number*/circleRecognition: true,
})
