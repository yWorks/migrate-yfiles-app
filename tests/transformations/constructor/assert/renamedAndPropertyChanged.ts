import { OrganicLayout } from 'yfiles'

const ol = new OrganicLayout({
  /*TODO-Migration Possible breaking change: Type of newMember on OrganicLayout has changed from boolean to number*/newMember: true,
})
