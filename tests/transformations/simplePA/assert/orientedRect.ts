import { OrientedRectangle } from 'yfiles'

const orientedRect = new OrientedRectangle()

/*TODO-Migration Rotation for angle has changed negate the angle and check the resulting visual*/orientedRect.angle = 5

console.log(/*TODO-Migration Rotation for angle has changed negate the angle and check the resulting visual*/orientedRect.angle)